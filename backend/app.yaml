runtime: nodejs22
env: standard

# Set env_variables via Google Cloud Secret Manager or `gcloud app deploy --set-env-vars`
# Do NOT hardcode secrets here.
# env_variables:
#   CLOUDINARY_CLOUD_NAME: (from Secret Manager)
#   CLOUDINARY_API_KEY: (from Secret Manager)
#   CLOUDINARY_API_SECRET: (from Secret Manager)
