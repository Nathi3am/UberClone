{
  "note": "Placeholder only - do NOT commit real Firebase service account JSON to the repository.",
  "instructions": "Store the real service account JSON in a secure secret store or on the server filesystem outside the repository. If this file was ever committed, rotate the service account key in the Firebase console immediately and delete any backups that contain the key."
}
